
filter:
    excluded_paths: [node_modules/*, test/*, sim-soft/*, frontend/*]

checks:
    javascript: true

build:
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
